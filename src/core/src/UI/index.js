import { disableRect, enableRect } from './rect';
import { disableSpan, enableSpan, createSpan } from './span';
import { disableRelation, enableRelation, createRelation } from './relation';
import { disableViewMode, enableViewMode } from './view';

export default {
  disableRect, enableRect,
  disableSpan, enableSpan, createSpan,
  disableRelation, enableRelation, createRelation,
  disableViewMode, enableViewMode
};
