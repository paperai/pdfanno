import { disableRect, enableRect } from './rect';
import { disableSpan, enableSpan, createSpan, getRectangles } from './span';
import { createRelation } from './relation';
import { disableViewMode, enableViewMode } from './view';

export default {
  disableRect, enableRect,
  disableSpan, enableSpan, createSpan, getRectangles,
  createRelation,
  disableViewMode, enableViewMode
};
